## Summary
- rebuild UncleCode into a work-first local agent shell: direct default work entry, visible execution trace, persisted work sessions, guidance/skills loading, and compact context/auth surfaces
- ship a real auth product surface: browser OAuth, device OAuth, API-key login/logout, Codex auth reuse, auth-source visibility, and inline auth handling that no longer crashes or misleads the work shell
- close rebuild plan gaps around setup/doctor/release surface/performance with provenance coverage, machine-readable perf surfaces, discovery caching + reload, and fast operator startup paths

## Key Product Changes
- `unclecode` now opens the work shell by default
- `unclecode tui` maps to the real work surface; old launcher is now `unclecode center`
- work shell exposes model / reasoning / auth / tool-use / orchestration trace
- AGENTS.md / CLAUDE.md and project skills are loaded into runtime context
- `/skills`, `/skill <name>`, `/reload`, `/doctor`, `/auth status`, `/auth login`, `/auth logout`, `/mcp list`, `/mode status` supported inline
- session center is redesigned as a secondary resume/auth launcher surface
- pasted image-file paths become safe image attachments in the OpenAI work shell
- slash suggestions/autocomplete, compact context panels, and auth launcher cards are added
- browser OAuth/session-center behavior now matches CLI auth behavior
- auth route messaging is explicit across launcher and auth-status surfaces (`Route · Browser OAuth` vs `Route · Device OAuth`)
- setup/doctor/config/sessions hot paths now use lightweight fast paths

## Competitive Follow-through
- investigated Open Multi-Agent and translated findings into backlog items around trace contracts, scheduler strategy, loop guardrails, and clearer orchestrator boundaries

## Test Plan
- [x] `npm run build --silent`
- [x] `npm run check --silent`
- [x] `node --conditions=source --import tsx --test tests/commands/*.test.mjs tests/orchestrator/*.test.mjs tests/tui/*.test.mjs tests/work/*.test.mjs tests/contracts/*.test.mjs tests/providers/*.test.mjs`
- [x] `node --test tests/integration/*.test.mjs`

## Fresh verification snapshot
- source suites: `312 / 312 pass`
- integration: `48 / 48 pass`
- targeted auth/work-shell closeout verify: `64 / 64 pass`

## Fresh hot-path timings
- `setup` → `0.08s`
- `auth status` → `0.07s`
- `doctor --verbose` → `0.24s`
- `mode status` → `0.13s`
- `sessions` → `0.14s`
- `config explain` → `0.08s`

## Caveats
- current worktree is large and already on `main`, so git integration should be handled deliberately rather than automatically
- brand-new browser OAuth initiation still depends on `OPENAI_OAUTH_CLIENT_ID` being available in the environment
